1.su命令
  switch user
  su -l "user"
2.passwd  修改密码   root用户密码没有复杂度要求
 密码复杂度要求：
    使用四种类型至少三种
    足够长
    使用随机字符串
    定期更换
    循环周期足够大
3.IDE:集成开发环境
  DLL:动态链接库
4.认证机制：Authertication
  授权：Authorization
  审计：Audition
5.文件系统     文件路径分为绝对路径与相对路径    文件名并不是文件数据
  绝对路径：从根目录开始，/开头
  相对路径：从当前目录开始，.开头
  命令类型分为内置命令和外部命令  【】表示可省略 <>表示必须存在 …表示可以出现多次
  内置命令：shell内置
  外部命令：在文件系统的某个路径下有一个与命令名称相应的可执行文件
6.ls命令
   （1）-l:显示完整信息 

 其中
      -：普通文件
      d: 目录文件
      b: 块设备文件（block）
      c：字符设备文件（character）
      l: 符号链接文件（symbolic link file）
      p: 命令管道文件（pipe）
      s: 套接字文件(socket)
各个参数说明：
  文件权限：9位  3位一组  rwx (读写执行）
  文件硬链接次数
  文件所有者
  文件所有组
  文件大小 单位 bit （如果换算单位  命令 -h）
  时间戳  (最后一次被修改、访问、改变的时间）
  文件名
  （2) -a 显示以'.'开头的隐藏文件
           . 表示当前目录
          .. 表示当前目录的上一级目录
   

   （3）-A   不包含 '.' '..'

   (4)  -d   显示目录的自身属性  
  （5）-i 显示文件缩影节点名
   (6) -r 逆序现实文件
  （7）-R 递归显示 (当前目录下的所有子目录以及以下的子目录）
7.cd命令
  进入指定目录
  cd - ：在当前目录和前一次所在目录来回切换
8.环境变量：命名的内存空间
 PATH:使用冒号分割的路径
9.hash命令：缓存、记录使用过命令的路径  实现系统加速  hash的时间复杂度为O(1)
10.查看时间的命令
  date:查看系统时间
  clock:查看硬件时间
                                 系统断电重启后，会先通过RTC读取硬件时间，读取成功后内核模拟振动时间为系统时间
11.ntp命令    network time protocol
12.命令使用帮助的命令
   内部命令： help command
   外部命令： command --help
13.命令手册 mananl  命令为man command
   该命令分章节  (1) 用户命令
                 (2) 系统调用
                 (3) 库调用
                 (4) 特殊文件
                 (5) 文件格式
                 (6) 游戏
                 (7) 杂项
                 (8) 管理命令
14.whatis command  该命令可以查看命令出现在哪个章节
15.file命令：查看文件格式
